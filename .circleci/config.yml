version: 2.1
orbs:
  win: circleci/windows@2.2.0
jobs:
  deploy:
    executor:
      name: win/default
      shell: powershell.exe
    steps:
      - checkout
      - run: npm start publish-win64
workflows:
    deploy:
      jobs:
        - deploy